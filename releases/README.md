# Releases

This directory stores repository-version baselines and historical artifacts.

- `0.0.14/` — known-working pre-core implementation, corresponding to the historical v14 distribution supplied from the deployed version.

Active development is tracked by the top-level `VERSION` file and `docs/VERSIONS.md`.
